import ThemeButton from './Button'

export default function Card() {
  return (
    <div className="card">
      <p className="card-text">Layout → Card → Button (useContext ở Button)</p>
      <ThemeButton />
    </div>
  )
}
